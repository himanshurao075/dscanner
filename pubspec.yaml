name: dscanner
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.6+4
  image: ^4.0.15
  path_provider:
  camera: ^0.10.3+2
  camera_camera:

  opencv_4: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
